{% extends 'auth.html.twig' %}
{% trans_default_domain 'app_auth_reset_reset' %}

{% block title %}{{ 'Password Reset'|trans }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card mx-4">
                    <div class="card-body p-4">
                        {% include '_flashes.html.twig' %}

                        <h1>{{ 'Password Reset'|trans }}</h1>

                        {{ form_start(form) }}

                        <div class="input-group mb-4">
                            <div class="input-group-prepend">
                                <span class="input-group-text"><i class="icon-lock"></i></span>
                            </div>
                            {{ form_widget(form.password, {'attr': {'placeholder': 'Password'|trans}}) }}
                            {{ form_errors(form.password) }}
                        </div>

                        <button type="submit" class="btn btn-block btn-success">{{ 'Set Password'|trans }}</button>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
