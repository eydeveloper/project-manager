{% extends 'base.html.twig' %}

{% trans_default_domain 'app_work_members' %}

{% block title %}{{ 'Groups'|trans }}{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">{{ 'Home'|trans }}</a></li>
        <li class="breadcrumb-item"><a href="{{ path('work') }}">{{ 'Work'|trans }}</a></li>
        <li class="breadcrumb-item"><a href="{{ path('work.members') }}">{{ 'Members'|trans }}</a></li>
        <li class="breadcrumb-item"><a href="{{ path('work.members.groups') }}">{{ 'Groups'|trans }}</a></li>
        <li class="breadcrumb-item active">{{ 'Create'|trans }}</li>
    </ol>
{% endblock %}

{% block body %}

    {{ include('app/work/members/groups/_tabs.html.twig') }}

    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">{{ 'Create'|trans }}</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}